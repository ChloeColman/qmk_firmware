#pragma once

// Quick tap (tap a dual-role key, press it again within the term, and the
// second press is a tap that auto-repeats instead of a hold) is decided per
// key in miryoku.c: on for the thumb layer-taps, off for home row mods.
#define QUICK_TAP_TERM_PER_KEY
